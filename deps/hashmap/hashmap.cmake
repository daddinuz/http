set(DEPENDENCIES "m")

set(ARCHIVE_NAME hashmap)
if (NOT DEFINED ARCHIVE_PATH)
    set(ARCHIVE_PATH deps/${ARCHIVE_NAME})
endif ()

file(GLOB ARCHIVE_HEADER_FILES ${ARCHIVE_PATH}/*.h)
file(GLOB ARCHIVE_SOURCE_FILES ${ARCHIVE_PATH}/*.c)
add_library(${ARCHIVE_NAME} ${ARCHIVE_HEADER_FILES} ${ARCHIVE_SOURCE_FILES})
target_link_libraries(${ARCHIVE_NAME} PRIVATE ${DEPENDENCIES})
include_directories(${ARCHIVE_PATH})
